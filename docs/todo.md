# 产品待办

最后更新：2026-08-20

本文档只保留尚未完成的产品问题；功能完成并通过对应验收后直接删除，不在此维护历史完成记录。

## TODO-MCP-LOGS-002：让 MCP 提供完整的只读运行日志

需求状态：`待实施`

优先级：`P0`

### 已确认的问题与边界

- 当前 MCP 的 `diagnostics_query` 只读取有界内存中的结构化诊断事件，并不是完整应用日志。
  默认返回 300 条、单次最多 500 条；容量淘汰或应用重启后无法继续读取更早记录。
- HTTP 与 Socket 抓包工具可以读取仍在抓包存储中的原始数据、写出数据、解析字段、协议视图和
  命中规则，但抓包不能替代应用运行日志。
- 当前 MCP 未提供 Rust/Tauri 完整运行日志、标准输出与标准错误、完整错误因果链、崩溃堆栈、
  WebView 控制台日志或 Android logcat。仅依赖现有 MCP 时，AI 无法完整分析启动失败、底层崩溃、
  已被内存容量淘汰的历史故障和部分跨进程问题。
- MCP 只允许读取和提出操作建议，不得直接修改配置、启动或停止入口、执行规则、导入证书，或对
  应用产生其他写操作。

### 目标设计

- 将运行日志与抓包、结构化诊断明确分离：抓包回答“线路上传输了什么”，诊断回答“已识别到什么
  阶段和结果”，运行日志回答“应用内部按时间实际发生了什么”。三类证据通过入口、连接、交换、
  请求或规则 ID 关联，但不互相伪装。
- 新增只读 `application_log_query` MCP 工具，支持按时间范围、严重级别、模块、数据平面、入口、
  连接/交换 ID 和关键字过滤，并使用稳定游标分页；返回总量、保留范围和是否截断。
- 新增只读 `application_log_get` MCP 工具，按稳定日志 ID 返回完整记录，包括时间、级别、模块、
  阶段、消息、结构化上下文、错误码、完整错误因果链和可用的崩溃/回溯信息。
- 日志使用有界持久化存储，应用重启后仍可查询；容量、保留天数和磁盘占用必须可见。达到上限时
  按明确规则淘汰最旧记录，并记录淘汰范围，不能让 MCP 把“没有查到”表述为“没有发生”。
- 如需诊断 Android Companion，再新增只读 `android_logcat_query`，按设备序列号、时间、进程、
  缓冲区和级别过滤；不得把 logcat 混入桌面应用日志，也不得对未选择的设备执行控制操作。
- 设置页 MCP 说明需列出访问地址、连接方式、工具清单、数据保留边界和示例问题，让用户能够直接
  复制配置，并理解 MCP 只能读取证据和给出建议。

### 验收测试

- 制造应用启动、入口启动、HTTP、Socket、规则、协议包、证书、SQLite、Android 与 MCP 自身错误，
  MCP 能按时间和关联 ID 查询到完整错误链，并能继续跳转到对应抓包或结构化诊断。
- 应用重启后，保留期内日志仍可读取；跨页查询无重复或遗漏，时间边界与游标稳定，达到容量上限时
  淘汰行为和截断提示准确。
- 大日志、Unicode、多行错误、嵌套原因链和并发写入不破坏排序、分页或 MCP 响应；单次响应超过
  MCP 输出预算时返回可继续分页的明确结果，不截断成无效 JSON。
- MCP 的全部日志工具保持只读；自动化测试证明调用前后 Workspace、入口、规则、协议包、证书、
  设置、Android 状态和数据库业务数据均未发生变化。
- 设置页展示的 MCP 地址、配置示例和工具说明与运行时实际返回一致；使用外部 MCP 客户端完成一次
  “发现故障→查询日志→关联抓包→给出操作建议”的端到端验证。

## TODO-SOCKET-CAPTURE-004：让 Socket 透明转发流量进入抓包 UI

需求状态：`待实施`

优先级：`P0`

### 已确认的问题与根因

- 透明转发当前走 `relay_bidirectional_with_progress`，只累计应用→上游服务、上游服务→应用的
  字节数并发布运行诊断，没有创建或注入 Socket 抓包上下文，因此抓包查询、事件刷新和详情 UI
  都没有可展示的数据。这不是前端刷新问题。
- 按协议转发和本机应答会经过协议处理器及现有抓包发布器；透明转发刻意绕过协议包、字段结构、
  规则与协议视图，所以不能复用现有“转发报文/本机应答”数据模型伪造一条协议报文。
- TCP `read`/`write` 的分段不是业务报文边界。一次读取可能只包含半条消息，也可能包含多条消息；
  UI 必须明确显示“字节流片段/连接流量”，不能把内部缓冲区边界称为报文。

### 目标设计

- 在 Socket 抓包模型中新增严格独立的 `TransparentFlow`（名称可在实现时按仓库约定调整）变体，
  与按协议转发、本机应答保持 closed union 隔离。该变体只保存：工作区、入口、运行代次、连接、
  双向序号、方向、已成功写出的原始字节、字节数、截断信息和时间；不包含协议包、字段结构、
  字段值、命中规则或协议视图。
- 在 proxy 的通用字节泵中定义中立、只读的“成功写出字节观察”接口；proxy 不依赖 application、
  SQLite 或 UI DTO。infrastructure 负责把观察事件适配到现有有界、非阻塞的抓包发布器，保持依赖
  倒置并避免把持久化逻辑塞进转发核心。
- 只在字节已经成功写入对端后记录对应范围。仅从源端读到但没有写出的字节不得出现在成功流量中；
  部分写入后失败时只记录实际写出的前缀，并保留关联的失败诊断；EOF 或空读取不创建空记录。
- 透明转发仍是完全透明的 byte-for-byte 路径：抓包观察不得调用协议包、Rhai、字段规则或协议视图，
  也不得修改、等待持久化或重新分块线路数据。SQLite 慢、抓包队列满或 UI 未打开时仍优先保证转发；
  若观察数据因有界容量被丢弃，必须提供可见的丢弃计数/诊断，不能静默制造“没有流量”的假象。
- 抓包列表按连接聚合展示双向累计字节和最近时间；详情按连接、方向和序号展示有序的字节流片段，
  默认提供 Hex/原始字节视图。产品文案使用“应用→上游服务”“上游服务→应用”“透明转发流量”，
  不显示 `Listener` 等实现术语，也不提供不存在的协议字段视图。
- 清空抓包、切换工作区和运行代次继续复用现有 generation/owner 边界；旧连接的迟到观察不得在清空后
  复活，也不得混入新入口运行代次。

### 实施边界

1. proxy：在双向 copy/write 成功边界发布有序观察事件，并保持 half-close、超时、取消和背压语义不变。
2. infrastructure：为透明转发注入抓包适配器，复用现有有界发布队列和 SQLite drain；新增透明流量
   持久化、分页、详情和清理实现。
3. application/Tauri：扩展严格 tagged union、分页/详情 DTO 和事件投影；透明流量字段不得借用协议包字段。
4. frontend：在现有 Socket 抓包列表和详情中增加透明流量分支、双向字节统计及 Hex 视图；按协议数据
   的协议视图和规则命中展示保持不变。

### 验收测试

- 真实 TCP 覆盖应用→上游服务、上游服务→应用、双向同时传输、粘包、拆包、大于内部缓冲区的数据、
  half-close、取消、读写超时和连接失败；线路收到的字节必须与抓包记录拼接后的已写出字节完全一致。
- 无协议包、无字段规则时透明转发仍可抓取；抓包详情只显示原始/Hex，不出现字段结构、协议视图或命中规则。
- 写失败只记录成功前缀并关联详细失败原因；读取后零写出、空流和未建立连接均不得生成伪成功记录。
- 抓包队列饱和、SQLite 暂停、UI 关闭时转发行为和吞吐不依赖抓包消费者；容量丢弃有稳定计数和可见诊断。
- 清空抓包与连接并发、停止后迟到事件、工作区切换和入口重启均不复活旧数据或串到新运行代次。
- 前端列表、筛选、分页、详情关闭/焦点恢复和窄窗口布局覆盖透明转发；定向 Rust/TypeScript 测试及
  受影响覆盖率门禁通过后，再做真实客户端与 Windows 产物验证。

## TODO-RELEASE-QA-007：补做正式版完整回归与视觉验收

需求状态：`待实施`

优先级：`P1`

### 暂缓范围

- 当前优先通过 Windows `build-only` 快速生成测试产物；本次协议包列表行样式只执行最小前端回归，不以此
  代替正式版完整质量门禁。
- 在发布 1.0 前补跑前端全量测试、TypeScript 类型检查、Lint、Rust workspace tests、Clippy、覆盖率策略、
  架构边界、源码尺寸与差异检查。
- 在 Windows 测试产物上检查协议包列表的表头、每一行、悬停、键盘焦点、首行/末行圆角、长名称、状态徽标、
  HTTP/Socket 切换以及窄窗口布局；确认行按钮不再出现独立圆角卡片。
- 复核 Windows MSI、NSIS 与 portable ZIP 的启动、协议包导入/启用、入口选择和基础转发流程，再决定正式
  1.0 发布。
