# TASK-20260903-008：新建规则立即显示完整条件与动作表单

- 任务 ID：TASK-20260903-008
- 状态：已完成
- 任务日期：2026-09-03
- 创建时间：2026-09-03 16:13:07 +08:00
- 开始时间：2026-09-03 16:13:07 +08:00
- 最后更新时间：2026-09-03 16:20:43 +08:00
- 完成时间：2026-09-03 16:20:43 +08:00
- 创建路径：`docs/tasks/pending/2026-09-03/show-complete-rule-form-before-metadata.md`
- 归档路径：`docs/tasks/completed/2026-09-03/show-complete-rule-form-before-metadata.md`
- 关键词：`规则`、`新建规则`、`表单`、`条件`、`动作`、`优先级`
- 任务优先级：低（局部 UI 显示门槛调整，不改变规则 Schema、保存校验或 Runtime）

## 背景与目标

用户要求新建规则页面不要等输入名称和优先级后才显示后续条件与动作，打开配置流程后应直接看到
完整表单。

目标：选择 Listener 和处理阶段、取得该阶段能力后，立即展示说明、匹配条件、对应动作和保存按钮；
规则名称与优先级仍由保存按钮统一校验，不再控制后续表单是否渲染。

## 范围、不在范围与确认记录

- 移除 `priority != null` 对 `RuleSinglePairEditor` 渲染的限制。
- 允许创建草稿在渲染期间携带尚未填写的 priority，但物化保存输入前必须再次严格校验。
- 增加空名称、空优先级时条件/动作已显示且保存仍禁用的回归测试。
- 更新用户操作说明、构建安装本地 App、保存证据并提交。
- Listener 和处理阶段仍必须先选择，因为条件/动作选项来自对应 Rust 能力；不伪造默认能力。
- 不修改默认 Listener/阶段、字段默认值、Rust 校验、规则持久化、MCP 或 Runtime。
- 2026-09-03：用户明确要求“一开始就显示全部，不要输入名称优先级后才显示后面的条件这些”。
- 未确认事项：零；“全部”按当前能力依赖解释为 Listener/阶段确定后完整展示，名称/优先级不再作为显示门槛。

## 需求就绪与问题分析

- 输入：新建规则，已选择 Listener 和处理阶段，名称为空、优先级为空。
- 预期：条件与动作表单可见，保存按钮禁用。
- 最小复现：选择 HTTP Listener 与 Proxy → Server，保持名称/优先级为空。
- 当前已验证：`RuleCreationEditor` 使用 `context && structure && priority != null` 控制 `RuleSinglePairEditor` 渲染；`ready` 同时校验名称和优先级并控制保存禁用。
- 已确认根因：展示条件错误地复用了保存就绪字段 `priority`，把内容可见性与提交有效性耦合。
- 影响：新建规则交互；已有规则编辑不受影响。
- 需求就绪：目标、范围、输入输出、示例和 PASS/FAIL 标准均明确；进入实现时间 2026-09-03 16:13:07 +08:00。

## 方案、任务与验收

- 最小改动：渲染只依赖 `context && structure`，创建元数据 priority 改为可空；生成保存输入时再次检查名称和 priority。
- 最优设计：拆分完整 FormDraft 与 ValidatedRuleSaveInput 类型。当前只涉及一个局部字段且既有 ready 校验清晰，新增跨组件模型会扩大范围。
- 采用最小改动，不增加依赖或默认值。

验收标准：

1. 新建规则选择 Listener/阶段后，即使名称和优先级为空，也显示 HTTP/Socket 规则内容、匹配条件和对应动作。
2. 名称或优先级缺失时保存按钮仍禁用，不会产生不完整 SaveInput。
3. 填写合法名称、非负整数优先级并完成条件/动作后，原保存路径保持可用。
4. 定向规则 UI 测试、typecheck、lint、正式 macOS App 构建和本机安装验证通过。

| ID | 内容 | 状态 | 验收 |
| --- | --- | --- | --- |
| T01 | 解耦内容渲染与元数据校验 | 已完成 | 空名称/优先级仍显示内容 |
| T02 | 回归测试与操作文档 | 已完成 | 保存禁用和原路径保持 |
| T03 | 构建、安装、证据与归档 | 已完成 | 新包运行并归档 |

- 对抗审查：低风险局部 UI 修改，并按用户当前连续交付中明确要求跳过。

## 实施、测试与完成总结

- `RuleCreationEditor` 只以已解析的 Listener/阶段上下文和规则能力控制完整编辑器渲染，不再以名称或优先级控制可见性。
- 创建草稿允许优先级暂时为空；物化保存输入时再次校验名称和非负安全整数优先级，保持 fail-closed。
- 回归测试覆盖名称与优先级为空时规则内容、匹配条件和对应动作可见，同时保存按钮禁用；规则 UI 定向测试 14/14 PASS。
- `deno task typecheck`、`deno task lint`、`git diff --check` 均 PASS。
- `deno task tauri build --bundles app` PASS；正式 App 已安装至 `/Applications/Intercept Proxy.app`，严格签名校验 PASS，运行 PID 为 `15686`。
- 实际安装界面验证 PASS：选择 `Payment DLL · HTTP` 和 `Proxy → Server` 后，名称与优先级保持为空，完整 HTTP 规则内容、匹配条件、对应动作已显示，保存按钮保持禁用。
- 测试证据：[RULE-FORM-IMMEDIATE-001](../../../testing/evidence/2026-09-03/TASK-20260903-008/RULE-FORM-IMMEDIATE-001/README.md)。
- CI、push、发布：NOT_RUN；不在本次授权范围。对抗审查：按用户明确要求跳过。
- 验收结果：PASS。
