# 准备步骤

1. 从仓库根目录确认 `inputs/task-related-files.txt` 的 29 个路径存在。
2. 比较七个活动 fixture 与 `resources/active-fixtures/` 副本内容。
3. Rust 命令始终从仓库根目录使用 `--manifest-path src-tauri/Cargo.toml`。

不需要启动 App、Listener、SQLite、Android、外部包或远程服务。
