# DOC-GOV-005：Agent 工作流治理规则验证

## 测试信息

- 任务 ID：`TASK-20260826-001`
- 用例 ID：`DOC-GOV-005`
- 执行时间：2026-08-26 10:37:12 +08:00
- 被测对象：当前 `AGENTS.md`、`.gitignore`、任务文档和 `docs/README.md` 任务入口
- 结果：PASS

## 输入和预期

- 输入：用户确认的流程优化要求，见 `resources/requirements.md`。
- 预期：存在需求就绪门禁、根因分析步骤、风险分级测试矩阵、高低优先级流程和统一锁工具方向；
  版本管理术语只出现在代码提交章节。

## 执行步骤

1. 在当前 `AGENTS.md` 查找新增流程标题、门禁和测试矩阵。
2. 确认代码提交章节以外不存在版本管理术语。
3. 检查高优先级条件、低优先级必要流程和风险升级条件一致。
4. 检查根目录锁路径已经作为本地运行状态排除。
5. 使用 `git check-ignore` 验证主锁和恢复目录样例实际命中排除规则。
6. 检查任务文件存在且 `docs/README.md` 只有一个对应入口。
7. 检查相关 Markdown 没有行尾空白。

复测命令见 `steps/replay.md`，本次原始输出见 `outputs/static-check.txt`。

## 实际结果

- 需求就绪门禁：PASS。
- 根因分析与事实/推断/未知分离：PASS。
- 风险分级最低测试矩阵：PASS。
- 代码提交以外的约束不包含版本管理术语：PASS。
- 高、低优先级及其流程和升级条件：PASS。
- 根目录锁本地运行状态排除：PASS。
- 全局锁工具化方向且不虚构现有脚本：PASS。
- 任务路径与待实现入口一一对应：PASS。
- 测试期间被测对象稳定：PASS。
- 行尾空白检查：PASS。

## 不适用项

- 产品源码、网络、协议报文、UI、数据库、构建：N/A，本任务只修改治理文档。
- CI：N/A，用户未要求执行 CI。
