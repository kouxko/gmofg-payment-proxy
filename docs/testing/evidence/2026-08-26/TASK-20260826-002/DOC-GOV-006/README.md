# DOC-GOV-006：快速配置验证治理规则

## 目的

验证当前权威文档已经建立独立 QV 通道，并能够防止证书、URL、TLS 和临时 Proxy 配置的快速检查
越级成为完整链路或正式任务验收结论。

## 环境

- 系统：Darwin 27.0.0 arm64
- Shell：zsh 5.9
- ripgrep：15.2.0
- jq：1.7.1-apple
- 执行时间：2026-08-26 12:34:00 +08:00

## 被测对象

- `AGENTS.md`
- `docs/README.md`
- `docs/testing/quick-validations/README.md`
- `docs/testing/quick-validations/TEMPLATE.md`
- `docs/tasks/completed/2026-08-26/rapid-configuration-validation-workflow.md`（测试时位于 pending，完成后原内容归档至此）

测试期间上述对象未发生变化。

## 前置条件

- 用户已确认独立快速验证通道、分层结论、轻量档案、清理检查和升级条件方向。
- 本测试只检查治理合同、模板、目录和链接，不执行真实证书、URL、后台或 Proxy 运行测试。

## 步骤与命令

完整复测命令见 [replay.md](steps/replay.md)。检查内容包括：

1. `AGENTS.md` 中 `10.5 快速配置验证` 唯一存在。
2. QV 与 TASK 的分流和升级条件存在。
3. QV 编号、`validation_id`、短时全局锁事务和索引生命周期完整。
4. 八个验证层级和四个结果状态在规则与模板中对应。
5. 两段独立验证不能证明完整 Proxy 链路。
6. 精确草稿字段、测试前状态、清理状态和保留对象均为强制记录。
7. 不允许隐藏关闭校验、替换资源、发送未授权业务请求或把 QV 直接升级为验收 PASS。
8. 任务入口、快速验证索引、模板路径和模板 JSON 可用。
9. 版本管理约束仍只位于独立代码提交章节。

## 实际结果

- 第一次和第二次检查：检查器分别在两个跨行文本谓词处退出 1；规则内容存在，但检查器把跨行句子
  当作单行匹配。
- 修正检查器：使用固定文本和跨行归一化判断，没有修改规则内容或预期结果。
- 首次整体对抗审查发现 6 个 P1：分流冲突、执行中断恢复、状态聚合、完整链路因果证据、环境信息和
  确定性场景不足；全部已修正并重新验证。
- 第二次整体对抗审查发现 2 个 P1：恢复事务锁边界不明确，以及保存的脚本未真实执行并发、失活恢复
  和双最终化竞争；已修正为三段式恢复事务和临时目录真实文件状态测试。
- 最终完整检查：全部静态检查 PASS。
- 状态场景验证：聚合、生命周期转换、已有任务分流和执行中升级全部 PASS。
- 真实文件状态验证：两个独立进程竞争分配得到唯一编号；`RESERVED` 与 `RUNNING` 执行者退出后，
  恢复所有权同时落盘到 metadata 和索引；原执行者回写被拒；两个最终化者只有一个成功；重复执行
  创建新 QV 并保存 `derived_from`，全部 PASS。
- 权威规则和快速验证索引在测试期间保持稳定。
- 静态输出见 [static-check.txt](outputs/static-check.txt)。

## 结论

结果：PASS。

当前规则可以区分观察性 QV 与正式任务，能够按资源、DNS、TCP、直接 TLS、Proxy 上游、下游
Listener、完整 Proxy 链路和应用协议逐层给出结论，并保留精确输入、环境、执行所有权、因果关联、
清理和复测证据。独立整体复审结论归档后才形成最终任务验收。

## 不适用项

- 真实证书、报文、TCP chunks、Frame、响应和截图：N/A；本任务只修改治理文档。
- 产品单元测试、构建、本地运行、真实设备和真实后台：N/A；不改变产品行为。
- CI：N/A；用户未要求，未执行。
