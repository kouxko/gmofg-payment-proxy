# DOC-GOV-004：单工作区全局任务锁规范验证

## 测试信息

- 任务 ID：`TASK-20260825-007`
- 用例 ID：`DOC-GOV-004`
- 执行时间：2026-08-25 23:57:15 +08:00
- 被测对象：当前 `AGENTS.md` 任务管理规则与 `replay/lock-protocol.sh`
- 结果：PASS

## 输入

- 用户确认的单工作区全局锁要求：`resources/requirements.md`
- 被测规则：仓库根目录 `AGENTS.md`
- 验收证明来源：当前权威文件内容、任务与索引对应关系、锁状态转换和复测实际输出。

## 执行与输出

- 复测步骤：`steps/replay.md`
- 隔离异常路径脚本：`replay/lock-protocol.sh`
- 原子锁行为原始输出：`outputs/lock-check.txt`

## 实际结果

- 全局锁位置、原子获取、完整任务管理事务范围和释放规则存在：PASS。
- 等待重试期间第二所有者无法获取同一锁：PASS。
- 错误所有者无法释放锁：PASS。
- 部分写入不一致时拒绝释放并保留锁，所有者修复后一致释放：PASS。
- 有效遗留所有者和缺失所有者记录均可按显式恢复流程接管：PASS。
- 恢复者移动旧锁后，普通获取者无法插入恢复窗口：PASS。
- 恢复完成并删除隔离标记后，普通获取者可以再次获取锁：PASS。
- 恢复者在创建恢复主锁前失活时，下一恢复者保持隔离标记并成功接管：PASS。
- 恢复者在创建恢复主锁后失活时，下一恢复者隔离遗留恢复主锁并成功接管：PASS。
- 连续两代恢复者在创建恢复主锁后失活时，每代使用唯一目录并由下一恢复者接管：PASS。
- 有效、缺失和损坏原所有者均写入明确的恢复来源状态：PASS。
- 恢复主锁在 `mkdir` 后无 owner 或 owner 损坏时，下一恢复者均可在外层恢复者失活和状态稳定后接管：PASS。
- 损坏的原 owner 与恢复主锁 owner 均逐字节保留，恢复链数组与两代历史目录一一对应：PASS。
- `recovered_from` 不匹配、多个外层隔离目录和唯一目标目录冲突均拒绝继续：PASS。
- 并行批次必须全部结束并完成集成验证后才可决定可选批次审查：PASS。
- 普通源码开发和耗时测试不占用任务锁：PASS。
- 明确不处理多 worktree、不同 clone 和跨机器协调：PASS。
- 主任务编号 `TASK-20260825-001` 至 `TASK-20260825-007` 无重复：PASS。
- 测试证据中的任务目录均对应已登记主任务：PASS。
- TASK-007 pending/完成状态与对应索引入口数量一致：PASS。

## 不适用项

- 网络报文：N/A，本任务不执行网络功能。
- UI 截图：N/A，本任务不修改 UI。
- CI：N/A，用户未要求执行 CI。
