# 用户确认要求

- 使用一把全局锁串行化多个 Agent 的任务管理操作。
- 只考虑同一工作区，不考虑多个 worktree、不同 clone 或跨机器协调。
- 保留现有按日期递增的任务编号。
- 将确认后的规则写入项目 `AGENTS.md`。
- Git 提交、提交历史、Commit ID、Git 状态和 diff 只用于变更管理和追溯，不得作为任何任务的验收结果。
- 并行执行任务时，等待同批全部任务完成并集成验证后，再决定是否执行可选对抗审查。
