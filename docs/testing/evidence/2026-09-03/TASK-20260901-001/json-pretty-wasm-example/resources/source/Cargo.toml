[package]
name = "intercept-proxy-json-pretty-component"
version = "1.0.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde_json = { version = "1.0.149", features = ["preserve_order"] }
wit-bindgen = "=0.48.1"

[workspace]
