# Retired shared test Root fixture

此目录保留已撤销共享测试 Root 的历史证书与私钥 fixture。生产 `Intercept Proxy` 不加载、
不嵌入也不分发这些资源；每个安装实例独立生成 Root CA。启动同步只读取公开证书来识别
Schema100 数据中的旧 Root；一旦命中即停止初始化并要求清除旧数据，不提供兼容迁移。

已撤销公开证书的 SHA-256 指纹为：

```text
B4:72:77:A5:8D:81:AD:EB:3C:CE:59:7A:15:58:85:4D:AB:3D:0B:30:AB:CE:15:06:5A:FB:73:33:9B:CB:D7:4C
```

旧签发私钥已经公开且不能撤回。任何客户端和系统信任库都必须删除该旧 Root；
它不得用于生产、预生产、真实商户或任何非隔离环境。
