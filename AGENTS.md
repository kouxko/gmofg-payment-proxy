# Intercept Proxy Agent 执行规范

本文件是 `/Users/codin/Code/gmofg-payment-proxy` 的仓库级 Agent 治理规则，适用于本目录及其
所有子目录。目标是在不依赖临时会话上下文的前提下，让需求、实现、测试、审查、文档、交付和
归档具备可复现证据。

## 1. 规则优先级

发生冲突时按以下顺序执行：

1. 系统、平台和用户当前明确要求。
2. 破坏性操作、外部生产操作和权限边界。
3. 本文件规定的 CI、外部代码发送和发布限制。
4. 本文件规定的任务登记、记录、日期目录、归档和完成索引。
5. 当前启用的外部 Skill 流程。
6. 本文件的默认执行过程。

外部 Skill 可以替换更专业、等价或更强的执行过程，但不能改变任务档案方式、权限边界和最终
完成证据。Skill 自带任务格式只能作为附加文件，不能替代本项目任务文档。

## 2. 默认执行边界

- 明确、低风险、可逆的本地调查、编辑、测试和审查应自动完成。
- 需求不清晰时不得默认实现；先只读调查，再使用相关 Skill 或向用户提问。
- 不得覆盖、撤销或把用户及其他 Agent 的无关修改纳入当前任务。
- 不得自动向外部仓库发送代码、创建 PR、发布、部署或触发 CI。
- 本项目测试数据允许完整打印、显示和保存，不得因自行推定的隐私要求而删减测试证据；更高优先级
  的平台安全规则仍然有效。
- 不得增加需求之外的兜底、降级、静默忽略、默认成功或双实现路径。

## 3. 执行前读取项目历史

除纯格式化或不涉及项目状态的简单回答外，每个任务开始前必须读取相关项目历史：

1. 提取项目路径、当前工作状态、模块、文件、符号、协议、错误和任务类型。
2. 按当前项目路径和关键词查找历史记录。
3. 默认读取 1 至 3 个最相关的会话摘要。
4. 只有需要精确用户决定、错误、命令或证据时才读取对应原始会话。
5. 禁止无目标扫描全部会话，避免输出截断和跨项目污染。
6. 历史信息只提供连续性；当前源码、配置和新测试是最终事实来源。

历史信息必须分级：

- `当前已验证`：本次已经重新确认。
- `历史已验证`：过去有证据，本次尚未重验。
- `历史观察`：来自 UI、日志、截图或操作，只用于定位。
- `历史方案`：讨论或计划过，未证明实现。
- `推断`：有依据但不是已验证事实。
- `未知`：证据不足。

## 4. 需求零假设

禁止因为“通常如此”“框架默认”“实现方便”或“看起来合理”自行补全需求。以下内容不明确时必须
停止相关实现并确认：

- 功能范围、UI 行为、API 字段、协议格式、编码和 Frame 边界。
- 默认值、空值、超时、重试、错误返回、回退和兼容策略。
- 数据迁移、持久化、公共接口、数据库 Schema 和旧实现删除。
- 安全校验、性能限制、日志内容、代码交付、CI 和发布。
- 是否扩大到关联模块或顺带修复其他问题。

处理顺序：

1. 检查当前源码、任务文档、历史决定和官方合同。
2. 使用合适的需求访谈、架构、研究或领域 Skill。
3. 若仍有多个合理答案，列出选项、影响和推荐理由并询问用户。
4. 用户确认前只允许处理不依赖该决策的小任务。
5. 将问题、选项和确认结论写入任务文档的“需求确认记录”。

可以提出推荐，不能把推荐当成确认。

### 4.1 需求就绪门禁

非简单任务只有同时满足以下条件，才能从需求讨论进入实现：

- 问题、用户目标和成功结果已经明确。
- 范围与不在范围已经明确。
- 输入、输出、状态变化和适用的错误行为已经明确。
- 至少有一个可以复现或说明预期行为的具体示例；确实不适用时记录原因。
- 验收标准能够通过当前事实或可重复执行的测试直接判断 PASS/FAIL。
- 会改变实现方向的未确认事项已经清零。

任一条件不满足时，任务保持 `待确认`；只允许继续调查、制作复现、补充证据或处理不依赖该决定的
独立事项，不得开始相关实现。任务文档必须记录需求就绪检查结果和进入实现的时间。

需求进入实现后发生变化时，不得直接覆盖原结论。任务文档追加记录原需求、变化内容、变化原因、
影响范围、失效的验收项、新验收项和用户确认结果；受影响实现必须重新经过就绪门禁。

### 4.2 问题与根因分析

修复缺陷、异常、回归、性能问题或不符合预期的行为前，任务文档必须分开记录：

1. 实际现象与发生环境。
2. 预期行为和依据。
3. 最小可复现步骤；无法复现时记录已尝试方法和缺失条件。
4. `当前已验证` 的事实和原始证据。
5. `推断`、`未知` 和仍需确认的边界。
6. 候选原因，以及每个候选原因如何被证明或排除。
7. 已确认根因、因果链、影响范围和正确修复边界。

未确认根因时，可以继续诊断、增加观测、补充测试或缩小范围，但不得把缓解症状、扩大重试、增加
兜底或绕过失败的修改描述为根因修复。确实只能实施临时缓解时，必须由用户明确确认其目标、期限、
限制和后续根因任务，并且仍不得把失败报告为成功。

## 5. 任务登记和目录

任何修改源码、配置、脚本、测试或项目文档的非简单任务，在修改前必须登记。

### 5.1 待实现任务

- 在 `docs/README.md` 的 `## 待实现任务` 添加唯一入口。
- 创建 `docs/tasks/pending/<创建日期>/<task-slug>.md`。
- 日期格式为 `YYYY-MM-DD`，任务文件名使用小写 kebab-case。
- README 每个待办必须且只能对应一个任务文档。
- 任务文档必须先于实现创建。
- README 待实现任务按创建日期倒序排列，同日按创建时间倒序排列。

### 5.2 任务信息

任务文档至少记录：

- 任务 ID：`TASK-YYYYMMDD-NNN`。任务 ID 只由主 Agent 分配；分配前扫描 pending、completed 和
  测试证据目录中的当日编号，选择下一个未使用编号并在创建任务文件时完成预留。子 Agent 不得
  自行分配任务 ID。
- 状态、任务日期、创建时间、开始时间、最后更新时间和完成时间。
- 创建路径、归档路径、关键词和任务优先级。
- 背景、目标、范围、不在范围、需求确认记录和未确认事项。
- 需求就绪检查；问题类任务还必须记录现象、预期、复现、事实、推断、未知、候选原因、根因和影响范围。
- 最小改动与最优设计、小任务列表、文档影响、测试计划和对抗审查计划。
- 实施记录、修改文件、附加文件、验收结果、测试结果、CI 情况和完成总结。

状态使用：`待确认`、`待实现`、`进行中`、`已阻塞`、`已完成`、`已取消`。

时间统一使用 Asia/Shanghai：`YYYY-MM-DD HH:mm:ss +08:00`。

### 5.3 任务优先级与流程等级

任务优先级只使用：`高`、`低`。用户明确指定时以用户结论为准；用户未指定时由主 Agent 根据已确认
范围提出判断并写明理由。存在多个合理等级且会改变执行流程时，保持 `待确认` 并询问用户，不得默认
归类。

满足以下任一条件的任务应建议为高优先级：

- 用户明确要求完整流程或标记为高优先级。
- 涉及安全、协议、加解密、持久化、数据库 Schema、公共接口、兼容迁移、并发、生命周期或资源所有权。
- 涉及跨层依赖、大规模架构调整、多个 Agent 共享合同、真实设备、真实外部链路、发布或生产影响。
- 失败可能造成数据错误、不可恢复状态、错误业务结果或较大回归范围。

只有范围隔离、低风险、可逆、不改变公共合同，且失败影响局限的任务，才可以建议为低优先级，例如
纯文档修正、格式整理、局部内部实现或已有充分测试保护的小改动。任务大小不能单独决定优先级。

高优先级执行完整流程：历史读取、任务登记、需求就绪、适用的根因分析、最小与最优方案比较、任务
拆分、实现、受影响层级测试与证据、文档同步、整体对抗审查和日期归档全部执行。

低优先级执行必要流程：任务登记、需求确认、最小改动分析、针对性验证、适用的文档同步和日期归档
必须执行；不适用的根因分析、架构设计、完整测试层级和整体对抗审查可以跳过，但必须在
任务文档逐项记录不适用原因、实际执行步骤和验收依据。低优先级一旦发现公共合同、跨模块影响、
并发/生命周期风险、数据风险或范围扩大，必须停止实现并重新确认优先级。只要本节上述任一高优先级
条件开始适用，无论是否在示例中重复列出，都必须立即停止并重新确认优先级。

### 5.4 单工作区全局任务锁

本项目只处理同一工作区内多个 Agent 的任务管理并发，不为其他工作区副本或跨机器提供协调保证。
任务编号继续使用 `TASK-YYYYMMDD-NNN`。

任何 Agent 修改任务管理元数据前，必须先获取仓库根目录下的全局任务锁：

```text
.task-manager.lock/
```

普通获取者在创建主锁前必须先检查是否存在
`.task-manager.abandoned.*/` 恢复隔离目录；存在时说明恢复事务尚未完成，必须等待。确认不存在
恢复隔离目录后，通过原子 `mkdir .task-manager.lock` 创建主锁；只有创建成功的 Agent 才能
继续获取锁。创建主锁后、写入所有者文件前必须再次检查恢复隔离目录；若此时出现隔离目录，当前
Agent 必须删除自己刚创建的空主锁并等待，不得写入所有者文件或任务元数据。

两次恢复隔离目录检查均通过后，扫描编号或修改任何任务文件前，必须创建固定文件
`.task-manager.lock/owner.json`，其格式为：

```json
{
  "schema_version": 1,
  "session_id": "当前 Agent 或会话的稳定标识",
  "process_id": 12345,
  "acquired_at": "YYYY-MM-DD HH:mm:ss +08:00",
  "operation": "create|update|close|recover",
  "task_id": null,
  "state": "active",
  "recovered_from": null
}
```

创建新任务而尚未分配编号时，`task_id` 必须为 `null`；编号分配后在首次任务文件写入前更新为实际
任务 ID。无法提供独立进程 ID 时 `process_id` 使用 `null`，不得填写共享宿主进程冒充锁所有者。
`owner.json` 写入失败时不得执行任何任务管理写入，并删除由当前 Agent 创建的空锁目录后报告失败。
写入后以及每次任务管理写入和释放前，Agent 都必须重新读取 `owner.json`，确认其中的
`session_id`、`process_id` 和 `acquired_at` 与自己获取锁时记录的值一致。锁已经存在时，其他 Agent
必须读取所有者信息、等待并重新尝试，不得绕过锁扫描编号、创建目录、编辑任务文件或修改索引。

以下操作必须使用同一把锁串行执行：

- 扫描、分配和预留任务 ID。
- 创建任务日期目录和 pending 任务文件，检查 task slug 与目标路径是否重复。
- 新增、修改或删除 `docs/README.md` 的待实现任务入口。
- 修改任务状态、时间、优先级、测试证据链接和其他任务管理字段。
- 在 pending 与 completed 之间移动任务文档。
- 更新 `docs/tasks/README.md` 完成索引和 `docs/testing/evidence/README.md` 测试索引。
- 检查任务 ID、路径、README 入口、完成索引、测试索引和移动后链接的一致性。

持锁范围必须覆盖完整任务管理事务：获取锁后重新读取当前文件并重新扫描编号，完成全部相关写入、
最终一致性验证后才释放锁。不得先分配编号后释放锁，再创建任务文件；不得在
关闭任务时先释放锁，再移动文件或更新索引。

普通源码、测试和非任务管理文档的调查与实现不占用任务锁。不得在持锁期间执行耗时的业务测试、
构建、CI、等待用户输入或整体对抗审查；这些步骤完成后，再单独获取锁执行任务状态更新或关闭事务。

释放锁前必须确认当前 Agent 是锁所有者，并且任务管理事务已经完成或仅由本事务产生的部分写入已经
撤销，一致性检查全部通过。释放时先删除自己的 `owner.json`，再删除空锁目录。所有者不匹配时禁止
释放。

任务管理事务失败后不得直接释放锁。锁所有者必须在持锁状态下选择一种确定结果：

- 完成剩余写入并通过全部一致性检查。
- 只撤销本次事务产生的部分写入，恢复事务开始前状态并通过全部一致性检查。
- 无法完成或恢复时，将 `owner.json.state` 改为 `blocked` 并保留锁，报告真实错误和部分状态，等待
  显式恢复；不得让其他 Agent 在不一致状态上继续任务管理操作。

不得根据锁存在时长自动删除遗留锁。主 Agent 只有在同时确认原 `session_id` 已经不活动、记录的
独立 `process_id` 已不存在或为 `null`，并确认任务相关文件当前没有继续变化后，才能显式恢复有效
但已遗留的锁。若 `owner.json` 缺失或损坏，主 Agent 必须重复读取锁目录和任务相关文件状态，确认
所有状态保持不变且没有 Agent 声明所有权后才能恢复；无法确认时保持阻塞并询问用户，不得猜测。

恢复时先将原锁目录原子移动到唯一的隔离路径
`.task-manager.abandoned.<时间>.<恢复会话ID>/`；只有移动成功的 Agent 获得恢复资格。该隔离目录
同时是恢复进行中的标记，普通获取者必须按前述两次检查退让。原所有者发现锁路径或所有者记录不再
匹配时必须立即停止，不得继续写入。恢复者必须在隔离目录中创建固定 `recovery-owner.json`，至少记录：

```json
{
  "schema_version": 1,
  "session_id": "恢复 Agent 或会话的稳定标识",
  "process_id": null,
  "acquired_at": "YYYY-MM-DD HH:mm:ss +08:00",
  "state": "active",
  "original_owner_state": "valid|missing|damaged",
  "original_owner": null,
  "original_owner_raw_path": null,
  "interrupted_recovery_locks": []
}
```

原 `owner.json` 有效时，`original_owner_state` 为 `valid`，`original_owner` 保存完整原记录，
`original_owner_raw_path` 为 `null`。文件缺失时状态为 `missing`，后两项均为 `null`。文件损坏时状态
为 `damaged`、`original_owner` 为 `null`，并把未经改写的原始内容保存到隔离目录内唯一文件，使用
`original_owner_raw_path` 记录其相对路径。`interrupted_recovery_locks` 初始为空；每次接管失活恢复者时
必须追加该代中断主锁的相对路径、`valid|missing|damaged` 所有者状态、有效所有者完整记录或损坏原文
相对路径。所有者判断必须使用当前锁文件和任务状态，不得使用无关的变更记录代替。

只有成功移动原锁的恢复 Agent 可以在对应隔离目录存在时创建新的主锁；它必须在新
`owner.json` 中将 `operation` 设为 `recover`，并在 `recovered_from` 精确记录自己创建的隔离路径。
恢复 Agent 未能创建主锁时必须保持隔离目录并等待，不得删除恢复标记。获得新主锁后，在其保护下
检查并完成或撤销原事务、验证一致性，再在当前任务文档记录恢复原因。验证完成后先删除隔离目录，
确认没有其他恢复隔离目录，再按所有权规则释放新主锁。隔离目录存在期间，除其对应恢复 Agent 外，
任何 Agent 都不得修改任务元数据。

恢复者自身失活时，隔离目录不得形成永久阻塞。主 Agent 按与遗留主锁相同的失活和状态稳定条件
检查 `recovery-owner.json`；文件缺失或损坏时使用相同的重复读取和无所有者声明规则。确认恢复者
已经失活后，通过把现有隔离目录原子重命名为新的唯一
`.task-manager.abandoned.<新时间>.<新恢复会话ID>/` 取得恢复资格。重命名前后路径都匹配恢复
隔离标记，任何时刻都不得先删除旧标记再创建新标记。只有重命名成功的 Agent 可以写入新的
`recovery-owner.json` 并继续恢复。

若失活恢复者已经创建新主锁，新恢复者必须先按 `valid|missing|damaged` 分类其 `owner.json`。有效记录
必须同时满足 `operation=recover` 且 `recovered_from` 指向当前唯一外层隔离目录；不匹配时保持阻塞。
若记录缺失或损坏，只有在已经按前述规则证明外层 `recovery-owner.json` 的恢复者失活、任务状态稳定、
当前只有一个外层隔离目录，并再次确认主锁状态未变化后，才能认定该主锁属于对应恢复事务；损坏内容
必须未经改写地保留。完成分类后，将该主锁原子移动到隔离目录内部的唯一
`interrupted-recovery-lock.<时间>.<恢复会话ID>/` 路径；外层隔离标记在整个过程中保持存在。路径不得
复用或覆盖。有效 `owner.json` 原样保留在该目录；缺失记录为 `missing`；损坏记录移为该目录内唯一
原始文件并记录相对路径。每次接管前必须枚举并保留已有的全部中断恢复锁目录，并确认它们与
`interrupted_recovery_locks` 一一对应；它们只作为恢复链记录，不重新取得主锁所有权。随后按前述
方式原子重命名外层隔离目录、写入包含完整恢复链的新恢复所有者、创建新主锁并处理原事务。连续
多次恢复者失活时重复相同步骤，为每一代创建唯一目录；只有最终恢复完成、一致性验证通过后才能
随外层隔离目录一起清理全部中断恢复锁。若 `recovered_from` 不匹配、存在多个外层隔离目录、恢复链
记录与目录不一致、唯一目标路径已存在、主锁状态变化或无法证明恢复所有者失活，保持阻塞并询问
用户，不得合并、覆盖、删除或猜测所有权。

上述自然语言锁协议是统一锁工具落地前的可执行合同，不得由各 Agent 自行简化或发明不兼容变体。
后续实现统一锁工具时，必须把获取、所有权校验、等待、释放、遗留恢复和恢复链测试封装为同一个
仓库入口，并通过本节全部状态转换用例；工具验收并写入本节后，Agent 必须只通过该入口操作锁，
不得继续手工创建、修改或移动锁文件。统一工具尚未存在或尚未验收时，不得声称它可用，本节合同
继续生效。

## 6. 实现前比较最小改动和最优设计

需求就绪检查通过后才能比较实现方案；问题类任务还必须先完成第 4.2 节根因分析。每个任务必须比较：

| 方案 | 必须回答的问题 |
| --- | --- |
| 最小改动 | 最少修改哪些文件，是否保持行为，是否增加重复、临时路径、兼容层或技术债 |
| 最优设计 | 正确职责、依赖方向、数据流、错误模型、资源所有权、测试边界和迁移方式 |

如果最小改动不增加重复、不破坏依赖方向、不掩盖根因且不会形成长期债务，应优先采用。

如果当前设计不符合合理架构，允许大规模修改，但必须：

- 说明现有设计问题和目标架构。
- 先用回归测试锁定行为。
- 拆分成可独立验收、可构建、可回滚的小任务。
- 每项完成后验证并保持可独立回退；是否执行小任务对抗审查按第 14 节判断并记录。
- 不得用一次不可独立验收的超大变更完成全部改造。

## 7. 整洁架构要求

- 领域层不依赖 UI、数据库、网络、Tauri 或具体运行时。
- 应用层编排用例和端口，不承载基础设施细节。
- 基础设施通过端口实现外部能力。
- UI 只消费应用层提供的状态和能力，不复制领域规则。
- 依赖指向稳定的内部抽象。
- 连接、任务、状态和资源必须有明确所有者及取消、关闭和清理路径。
- HTTP 与 Socket 保留协议边界；共享语义通过明确的领域或应用合同复用。
- 不得通过跨层调用、前端条件复制或长期双路径绕过架构问题。

Rust/Cargo 命令从仓库根目录使用：

```bash
--manifest-path src-tauri/Cargo.toml
```

## 8. 小任务与多 Agent

任务文档可以拆分多个小任务，每项记录 ID、依赖、是否可并行、负责人、状态和验收标准。

没有依赖且不修改同一文件、接口、数据结构或迁移的小任务可以由多个 Agent 并行。并行前必须明确：

- 文件或模块所有权。
- 输入、输出和验收标准。
- 依赖图和集成顺序。
- 共享文件冲突的上报方式。

不得并行处理尚未稳定的共享接口、Schema、协议合同、生命周期所有权或有顺序要求的迁移。

主 Agent 负责拆分、分配、集成、冲突处理、整体架构和最终验证。子 Agent 不得撤销他人修改、
扩大范围或静默处理共享冲突。

同一批并行小任务启动后，必须等待该批全部任务进入终态并由主 Agent 完成结果收集、冲突处理和
集成验证，才可以判断是否执行一次该批次的可选针对性对抗审查。只要仍有任一并行任务正在执行、
结果未收集或集成未完成，就不得提前开始该批次审查。某一并行任务失败或阻塞时，先等待其余任务
结束，再统一记录批次结果和决定是否审查；不得把局部完成当成整批完成。本规则不改变第 14 节按
任务优先级确定的任务级审查门禁。

任务登记、状态更新、任务归档和任务索引写入还必须遵守第 5.4 节的全局任务锁；主 Agent 身份不能
替代锁，也不得授权子 Agent 绕过锁。

## 9. 禁止额外兜底错误处理

禁止为了让流程继续而加入：

- 捕获所有错误后忽略或返回成功。
- 空对象、空列表、默认值或正常状态替代失败。
- 未约定的重试、降级、协议回退或旧实现回退。
- 验证失败后关闭安全检查。
- 数据缺失时伪造数据。
- 用日志代替错误传播。
- 将部分成功描述为完整成功。

错误应通过现有强类型错误模型在正确边界传播，并保持可观测、可测试。错误行为只能遵循已经确认
的需求或现有合同；合同明确规定 fail-closed 的边界继续保持 fail-closed。合同缺失或存在多种合理
处理时停止相关实现并询问，不得自行选择继续、关闭、重试或回退。无论采用何种合同，都不得把
失败报告为成功。只有明确需求或既有合同允许时才可回退，并必须写入任务文档和测试。

## 10. 测试、数据和可复用证据

测试不能只报告退出码。每个测试必须保存适用于该测试类型、足以复现和判断结果的材料：

- 目的、环境、前置条件、步骤、命令、被测对象的当前状态记录和执行时间。
- 输入、规则、配置和预期输出。
- 协议、网络和报文测试保存发送报文、TCP chunks、完整 Frame、Decode、Rules、Encode、Server
  实际收到的数据、响应和 App 最终收到的数据。
- UI 测试保存交互步骤、状态变化、可访问性结果和必要截图。
- 纯领域、单元、文档或静态检查只保存适用的输入、命令、输出和比较结果。
- 预期、实际、逐字节或逐字段比较、日志、截图和附加文件。

不适用的证据项必须记录为 `N/A` 并说明原因，禁止创建虚假报文、截图或占位证据。

测试范围按变更风险和受影响边界选择，不得让所有任务机械执行同一套测试，也不得以低层测试替代
已经受影响的高层行为。最低验证要求如下：

| 任务类型 | 最低验证要求 |
| --- | --- |
| 纯文档或治理规则 | 当前权威内容、结构、链接和对应关系检查 |
| 纯领域或小型逻辑 | 单元测试、边界值和适用的失败路径 |
| 配置或脚本 | 语法/静态检查、实际执行、输入和输出 |
| 公共接口或跨模块合同 | 单元、集成、调用方和兼容性验证 |
| 协议、报文或加解密 | 原始输入、Frame/字段解析、规则处理、编码输出和字节/字段比较 |
| 并发、资源或生命周期 | 状态转换、竞争、取消、超时、清理和重复执行 |
| 数据库、Schema 或迁移 | 升级、失败路径、恢复或回滚策略和数据一致性 |
| UI | 关键交互、状态变化、错误展示、可访问性和必要截图 |
| 外部服务、真实设备或真实链路 | 环境、连接参数、实际请求响应、日志和对应业务结果 |

一项变更同时命中多类时合并其要求。无法执行受影响层级时必须记录缺失环境、未验证结论和剩余风险，
不得用未受影响层级的 PASS 替代。

### 10.1 可复用向量

被测模块附近的 fixture/sample 是当前自动化回归使用的活动资源，应遵循现有约定，例如：

- `<module>/tests/fixtures/<case-id>/`
- `<package>/samples/<case-id>/`
- `scripts/tests/fixtures/<case-id>/`
- `test-support/fixtures/<area>/<case-id>/`

活动 fixture 代表当前合同下自动化测试使用的资源；证据目录中的 resources 是某次测试实际使用
资源的不可变快照。测试执行时允许把活动 fixture 复制到证据目录，但必须在 README 或 metadata
记录来源 fixture 的仓库相对路径、用例版本和实际使用方式。后续历史复现使用证据快照，当前持续
回归使用活动 fixture。fixture
行为发生变化时创建新用例或明确版本，不覆盖历史证据快照。

### 10.2 单次执行证据

当次证据保存到：

```text
docs/testing/evidence/<执行日期>/<任务ID>/<用例ID>/
```

每个证据目录只强制包含：

- `README.md`：目的、步骤、命令和人工可读结果。
- `metadata.json`：任务、用例、环境、时间、被测状态记录、结果、复测命令和不适用项说明。

以下文件仅在适用时保存：

- 协议或二进制测试的原始 `.bin` 与可读 `.hex`。
- 其他测试的文本或结构化输入输出。
- `expected.json`、`actual.json`、`comparison.json`。
- stdout、stderr、日志和截图。

不适用项只在 `README.md` 或 `metadata.json` 中记录 `N/A` 和原因，不创建占位文件。

测试向量必须提供可重复执行的入口。每次执行直接记录被测对象、任务相关文件或运行组件、配置、
资源版本、环境、执行时间和测试期间是否发生变化。需要证明具体文件内容时，在证据目录保存当次
实际使用的输入或资源快照。所有任务的验收都必须直接使用当前权威文件、实际输入输出、行为结果、
状态转换、构建/运行结果、索引关系和适用审查结论。

共享工作区进行验收测试时，主 Agent 必须暂停对被测对象的修改并协调其他 Agent 不修改相同文件、
接口、数据或运行环境。测试期间被测状态发生变化时，该结果不得作为完成证据，必须在稳定状态重新
测试。不得因为测试失败直接覆盖期望结果；只有需求确认变更后才能更新 golden/expected，并记录原因。

### 10.3 测试资源必须成为任务归档的一部分

测试实际使用的所有文件型资源必须随对应测试用例归档，不得只记录本机临时路径或在最终回复中
口头说明。测试用例证据目录在任务文档中建立稳定链接后，视为该任务完成档案的一部分，不得与
任务档案分离删除或移动。

按实际使用情况在用例目录中保存：

```text
docs/testing/evidence/<执行日期>/<任务ID>/<用例ID>/
├── README.md
├── metadata.json
├── resources/       # 用户提供或测试依赖的原始资源
├── inputs/          # 实际输入、请求、报文和配置
├── outputs/         # 实际响应、解析结果、日志和生成文件
├── steps/           # 前置条件、环境准备、成功步骤和清理步骤
└── replay/          # 可直接复测的脚本、命令或操作说明
```

只创建实际有内容的子目录，不适用的目录不创建，并在 README 或 metadata 中说明 `N/A`。

归档资源包括但不限于：

- 用户提供的证书、证书链、客户端身份、配置文件、协议定义和其他测试文件。
- 目标 Host、Port、SNI、TLS/mTLS 模式、认证要求、Listener、规则和协议包配置。
- 请求报文、响应报文、TCP chunks、完整 Frame、字段解析结果和修改前后数据。
- 测试工具及版本、启动参数、依赖服务、环境变量、准备命令和资源加载顺序。
- 从零开始能够成功复现的步骤、成功判定、失败输出、实际结果和清理或恢复步骤。
- 测试期间产生且用于判断功能的日志、截图、导出文件、数据库副本或其他文件。

远程服务、系统工具、运行时、硬件或其他不可复制依赖不要求复制其本体，但必须保存名称、版本、
来源、获取方式、启动方式、连接参数、必要配置、可用性检查和测试时实际状态，使后续执行者能够
重建或确认同等环境。

资源说明必须记录原始文件名、来源、用途、是否必需、使用位置和相对路径。不得只保存证书或报文
文件而遗漏如何加载、发送、解析和判断成功。用户提供的资源按实际使用内容完整保存；不得自行删减、
改写或用新生成资源替代后仍声称测试了原资源。

任务完成前必须确认任务文档已经列出所有测试用例和证据目录。缺少资源、前置条件、成功步骤、
实际输出或复测入口时，对应功能不得记为已验收。

### 10.4 后续任务必须引用和复用原测试

由既有测试延伸出的新需求、修改或回归任务，必须在任务文档中记录：

- 原任务 ID。
- 原测试用例 ID。
- 原证据目录和具体资源相对路径。
- 复用的前置条件、执行步骤和预期结果。
- 本次需求相对原测试改变了什么，以及哪些行为必须保持不变。

后续任务优先直接引用原测试资源，不复制产生多个无权威来源的活动副本。原测试归档一旦完成不得
覆盖、改写或删除。资源、步骤或预期结果发生变化时，创建新的测试用例或明确的新版本，在新用例
中记录 `derived_from`、变化内容和原因。`derived_from` 必须同时包含父任务 ID、父用例 ID 和父证据
相对路径，不能只使用可能在其他任务中重复的用例 ID。

原用例与派生用例的查询关系记录在独立、可追加的 `docs/testing/evidence/README.md` 测试索引中，
不得为了增加反向链接而修改原证据目录。索引至少记录任务 ID、用例 ID、证据路径、用途、状态和
父任务 ID、父用例 ID、父证据相对路径。

验证派生需求时按合同类型执行：

- 增量、兼容或回归任务：先重放原测试并证明原预期继续通过，再执行新用例。
- 明确替代旧合同：保留并引用原历史 PASS；在新任务中执行旧用例作为差异证据，允许其按已确认的
  新合同产生预期失败，再执行新用例证明新行为。

任何情况下都不能直接修改原证据，伪造成历史行为一直等于新合同。

最终反馈必须说明测了什么、输入是什么、解析结果是什么、实际输出是什么、证据保存在哪里以及
下次如何复测。

## 11. 证据层级

验收结论必须来自与需求直接对应的当前事实和可复现验证，例如权威文件内容、实际输入输出、行为
结果、状态转换、构建或运行结果、索引关系以及审查结论。变更保存动作不属于验收结果。

不同层级必须分开报告：

1. 源码与配置。
2. 单元或组件测试。
3. 类型检查、Lint 和静态分析。
4. 构建。
5. 本地运行或 UI Smoke。
6. Listener、外部包和网络连通性。
7. Frame、解析、加解密和字节保持。
8. 真实设备或真实样本交换。
9. CI、制品和发布。
10. 业务结果。

Listener 启动不等于外部包在线；外部包在线不等于 Frame 正确；测试通过不等于真实设备或业务
链路成功。

## 12. 文档同步

功能实现完成后必须检查所有相关文档。任务文档维护“文档影响分析”和“文档同步结果”，每份相关
文档标记为：`需要更新`、`无需更新`、`需要删除`、`需要合并` 或 `需要标记为已替代`。

按影响检查：根 README、`docs/README.md`、需求、用户操作、新人指南、架构、ADR、MCP、外部包、
测试矩阵、验证结果、Android 文档、示例 README、脚本说明、内嵌资源和任务档案。

- 文档必须与当前源码一致，补充用途、范围、错误、安全、兼容、迁移、限制和测试方式。
- 删除失效说明、旧命令、坏链接和重复内容。
- 同一事实只保留一个权威说明位置，其他位置链接引用。
- 不删除已完成任务、验收记录或有决策价值的 ADR；被替代 ADR 应保留并链接新 ADR。
- 删除文档前检查源码、`include_str!`、MCP、诊断、帮助和其他文档引用。

文档差异未处理时不得完成任务。

## 13. 外部 Skill 流程替代

使用 Skill 前必须完整读取其 `SKILL.md`。只有 Skill 与任务直接相关、覆盖同一目标且提供等价或
更强证据时，才可跳过重复默认步骤。

允许替代需求访谈、探索、架构规划、任务拆分、Agent 编排、测试设计、调试、审查和证据收集的
过程。不得替代：

- 任务登记、时间字段、目录、归档和完成索引。
- 用户确认、权限、安全、CI、外部代码发送和发布边界。
- 验收证据和最终完成状态。

在任务“实施记录”中记录 Skill 名称、来源、版本、替代步骤、理由、保留门禁、结果和证据。

## 14. 对抗审查

小任务的针对性对抗审查是可选项，不作为每次小任务完成的固定门禁。任务文档必须记录该小任务
是否执行审查；未执行时记录原因。

涉及安全、协议、持久化、数据库 Schema、公共接口、并发、生命周期、跨层依赖、大规模架构修改、
多个 Agent 共享合同或其他高风险边界的小任务建议优先进行针对性对抗审查，但仍是风险判断后的
可选项，不构成固定完成门禁。

并行批次的针对性对抗审查只能在该批次所有小任务结束、结果全部收集且集成验证完成后决定和执行，
每个并行执行中的小任务不得各自提前触发审查。审查对象是已经集成的完整批次结果；未执行时在任务
文档记录风险判断和原因。顺序执行的小任务仍可在自身完成并验证后单独决定是否审查。

全部小任务完成后，高优先级任务必须进行一次整体对抗审查，作为完成和归档的强制门禁；低优先级
任务由主 Agent 进行风险判断后决定是否执行，并在任务文档记录结论和理由。整体审查优先由未实现
该任务的独立 Agent 执行。

审查覆盖：

- 需求符合度和根因。
- 最小改动与最优架构选择。
- 依赖方向、重复逻辑和跨层调用。
- 错误、兜底、生命周期、并发、取消、超时和清理。
- 协议、安全、持久化和兼容边界。
- 测试充分性、测试证据和失败路径。
- 源码、文档、任务档案和当前实际变更内容的一致性。

审查结论为 `APPROVE`、`REQUEST CHANGES` 或 `BLOCK`；发现项按 P0/P1/P2 记录。执行了小任务
审查时，应在该小任务标记完成前解决相关发现并重新验证。整体审查发现的所有任务相关 P0、P1 和当前
范围内 P2 必须在任务归档前解决，修复后重新验证。超出范围的有效发现必须创建新的待实现任务。
高优先级任务或已经选择执行整体审查的低优先级任务，最终结论不是 `APPROVE` 时不得完成任务。

## 15. Git 提交

已登记并完成针对性验证的小任务可以创建本地 Git Commit；小任务对抗审查不再是固定提交前提。
顺序执行的小任务按以下循环；并行批次必须先等待全部小任务结束并完成集成验证，再统一执行第 4、
5 步：

1. 实现当前小任务。
2. 执行针对性测试并保存证据。
3. 更新任务和相关文档。
4. 判断是否需要小任务对抗审查，并在任务文档中记录决定和原因。
5. 若执行审查，修复相关发现并复验。
6. 只暂存当前小任务文件，检查待提交内容、无关文件和生成物。
7. 创建独立、可回滚的本地 Commit。
8. 更新小任务状态并自动进入下一项。

不得提交其他人或其他任务的修改。工作区无法安全分离时停止该提交，但可继续不冲突的小任务。

默认不 Push。

所有 Agent 共享同一工作区时，只有主 Agent 可以串行操作提交区、执行 `git add` 和创建 Commit；
子 Agent 只修改分配的文件并报告结果。只有子 Agent 位于明确隔离的独立工作区且主 Agent 已分配
提交所有权时，子 Agent 才可以创建自己的 Commit。

## 16. CI 规则

默认禁止触发任何远程 CI、检查、构建、发布或部署流程。

- 只有用户明确要求 CI 时才允许触发。
- 用户同时要求代码交付和 CI 时，只触发明确的 Windows Build Workflow。
- 不触发 Test、Lint、Check、Release、macOS、Linux、Android 或完整多平台 Pipeline。
- 触发前确认 Workflow 文件、名称、被构建版本和连带触发关系。
- 如果发送代码会触发 Windows Build 之外的 Workflow，不得发送或接受连带触发。
- 本地针对性测试不属于远程 CI，应按任务计划执行。

Windows Build 证据必须记录 Workflow、文件、被构建版本、Run ID、URL、开始/完成时间、最终状态、
Artifact 和是否触发其他 Workflow。

## 17. 任务完成与日期归档

任务完成必须同时满足：实现完整、对应优先级要求的测试、测试资源和证据完整、任务文档已链接全部
测试用例归档、对应优先级要求的审查门禁满足、相关文档已同步且任务文档已更新。用户要求 Windows
Build 时还必须取得其最终结果。

关闭顺序：

1. 获取第 5.4 节规定的全局任务锁，并在锁内重新确认任务 ID、pending 路径和目标归档路径唯一。
2. 更新任务状态、完成时间、验收、测试、修改文件、附加文件和剩余风险。
3. 将任务文档移动到实际完成日期：
   `docs/tasks/completed/<完成日期>/<task-slug>.md`。
4. 从 `docs/README.md` 的 `## 待实现任务` 删除任务。
5. 在 `docs/tasks/README.md` 的完成日期下增加索引，按日期和完成时间倒序排列。
6. 检查所有移动后的链接。
7. 验证任务和索引当前状态后释放全局任务锁。
8. 不触发 CI，自动进入下一项依赖已满足的任务。

完成索引必须记录完成时间、任务链接、实际实现功能、验收结果、优先级和关键词。已经归档的任务不得
移回 pending；发现新问题时创建新任务，并建立双向链接。

## 18. 停止条件

在以下任一条件满足时停止当前工作：

- 任务已经按优先级要求验证、审查并归档。
- 用户要求停止或取消。
- 存在未确认且会改变实现的需求或流程优先级。
- 需要破坏性、不可逆、外部生产或缺少权限的操作。
- 工作区冲突无法安全分离。

遇到可恢复的测试、实现或文档问题应继续修复，不得把困难本身当作完成或阻塞证据。
